/*global define*/

define(function () {
    'use strict';

    function ctrl($scope, $mainServices, $moreServices) {
    	
    }

    ctrl.$inject = ['$scope', '$mainServices', '$moreServices'];
    return ctrl;
    
});